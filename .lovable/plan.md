

## Remove border radius from BrandLogo

**What**: The `BrandLogo` component on line 28 has `rounded-sm` in its className. Remove it so the logo container has sharp corners.

**Change**: In `src/components/viewer/BrandLogo.tsx`, line 28, remove `rounded-sm` from the className string.

**Files modified**: `src/components/viewer/BrandLogo.tsx` only.

